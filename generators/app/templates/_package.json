{
  "name": "<%= projectName %>",
  "version": "0.1.0",
  "description": "<%= projectDescription %>",
  "repository": null,
  "readme": "README.md",
  "author": {
    "name": "<%= authorName %>",
    "email": "<%= authorEmail %>"
  },
  "license": "SEE LICENSE IN LICENSE.md",
  "devDependencies": {
    "gulp": "^3.0.0",
    "gulp-help": "^1.6.0",
    "gulp-watch": "^4.0.0",
    "mkdirp": "^0.5.0",
<% if (needsGlob) { %>    "glob": "^6.0.0",
<% } if (needsLodash) { %>    "lodash": "^3.0.0",
<% } if (needsRunSequence) { %>    "run-sequence": "^1.0.0",
<% } if (needsExec) { %>    "gulp-exec": "^2.0.0",
<% } if (needsRename) { %>    "gulp-rename": "^1.0.0",
<% } if (needsDateFormat) { %>    "dateformat": "^1.0.0",
<% } %>    "mdproc": "^0.1.0"
  }
}
